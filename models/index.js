const Message = require('./message');
const TextMessage = require('./text-message');
const ImageMessage = require('./image-message');
const Counter = require('./counter');

module.exports = {
    Message: Message,
    TextMessage: TextMessage,
    ImageMessage: ImageMessage,
    Counter: Counter
}